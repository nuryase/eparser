from setuptools import find_packages, setup

setup(
    name="eparser",
    packages=find_packages(include=["eparser"]),
    version="0.1.0",
    description="Library for EPUB handling and parsing",
    author="yasen",
)
